# Use Case Assistant Application Configuration

# Spring Shell Configuration
spring.shell.interactive.enabled=true
spring.shell.command.version.enabled=false

# Application Info
spring.application.name=use-case-assistant

# Logging Configuration
logging.level.root=INFO
logging.level.com.usecaseassistant=DEBUG

# Storage Configuration
usecase.storage.directory=${user.home}/.usecase-assistant/use-cases
